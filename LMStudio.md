1. Open the app
2. open the terminal and `~/.lmstudio/bin/lms bootstrap`
3. close it and open a new terminal
4. `lms server start --port 8081` (and check with `curl http://localhost:8081/v1/models`)
5. 



5. `lms server start --port 8082` (and check with `curl http://localhost:8082/v1/models`)
